/**
 * Domain Status Enum
 *
 * Defines the possible states of a domain in the system.
 */
export enum DomainStatus {
  /** Domain is active and operational */
  ACTIVE = 0,
  /** Domain is passive/inactive */
  PASSIVE = 1,
}
