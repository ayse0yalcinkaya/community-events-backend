/**
 * Notification Channel Enum
 *
 * Defines the notification channels available for user preferences.
 */
export enum NotificationChannel {
  /** Email notifications */
  EMAIL = 0,
  /** SMS notifications */
  SMS = 1,
  /** Push notifications */
  PUSH = 2,
}
