/**
 * Document Type Enum
 *
 * Defines the supported document types for generation.
 * Used by DocumentGeneratorService to determine which adapter factory to use.
 */
export enum DocumentType {
  PDF = 'PDF',
  EXCEL = 'EXCEL',
}
